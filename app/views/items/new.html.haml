%header
%body
  .SellPage
    .SellPage__Information
      .SellPage__Information__Image
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__ProductName
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__Description
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__Category
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__Brand
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__Condition
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__DeliveryFee
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__ShippingOrigin
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__DaysToShip
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit
      .SellPage__Information__Price
        = form_with url: items_new_path do |form|
          = form.text_field :image
          = form.submit       


